import '../stylesheet.css';

const OpenClaim = () => {

    return <div className="container">
    <h1>Open Claims</h1>
    <h3>This will display a list of claims with an open status. 
    Include a button to show the full claim details which when clicked will show the detail for that claim</h3>
    
</div>
}

export default OpenClaim;