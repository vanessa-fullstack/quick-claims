import '../stylesheet.css';

const DisplayClaim = () => {

    return <div className="container">
    <h1>Display Claims</h1>
    <h3>This will show the detail for a selected claim</h3>
    
</div>
}

export default DisplayClaim;